# Static JSON Endpoint

This repository hosts a minimal static JSON endpoint with GitHub Pages.

It tracks the circulating supply of the Amana Token (AMAA).

The JSON file is updated automatically by a scheduled GitHub Actions workflow and can also be refreshed manually from the Actions tab.

No frontend application, build step, or server is required.

Kondoo Digital GmbH
